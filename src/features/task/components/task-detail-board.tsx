import { Paper } from "@mantine/core";

const TaskDetailBoard = () => {
  return (
    <Paper h="100%" radius="lg" style={{ maxHeight: "calc(100vh - 120px)", minHeight: "calc(100vh - 120px)" }}>
      Hello World
    </Paper>
  );
};

export default TaskDetailBoard;
